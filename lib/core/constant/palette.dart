import 'package:flutter/material.dart';

class Palette {
  // Primary brand colors
  static const Color primary = Color(0xFF0255BD); // Updated Blue
  static const Color accentOrange = Color(0xFFF37021);
  static const Color accentGreen = Color(0xFF39B54A);
  static const Color accentTeal = Color(0xFF00AEEF);

  // Neutral colors
  static const Color white = Colors.white;
  static const Color black = Colors.black;

  // Text colors
  static const Color textPrimary = Colors.white;
  static const Color textSecondary = Colors.black87;
}
